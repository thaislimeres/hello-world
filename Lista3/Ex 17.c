#include <stdio.h>
main()
{
int y, ponto[y][3];

do
{printf("Insira a quantidade de funcionarios: ");
scanf("%d",&y);
if(y<=0)
    {printf("Insira uma quantidade maior que zero\n");}
}while(y<=0);

for(cont=0;cont<y;cont++)
    {printf("Insira a pontuacao dos meses de novembro, dezembro e janeiro\n");
    scanf("%d %d %d",pontos[y][]);
    printf("A pontuacao media e %d\n",pontos1+pontos2+pontos3/3)

    if(cont==0 || soma<pontos)
        {maior=pontos;}
    }

    printf("A maior pontuacao atingida")
}
